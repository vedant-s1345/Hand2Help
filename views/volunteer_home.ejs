<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Hello <%= volunteer.vusername %></h1>
    <% for (event of events){ %>
        <form method="post" action="/volunteer_home/<%= volunteer.vid %>/<%= event.event_type %>">
            <button type="submit"><%= event.event_type %></button>
        </form>
    <% } %>
</body>
</html>